- if @category.errors.any?
  |
    $("#message_errors_update#{@category.id}").html('#{ j render "shared/error_messages", object: @category }');
- else
  |
    $("#category#{@category.id}").replaceWith('#{ j render @category }');
